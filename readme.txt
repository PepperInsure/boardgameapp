index.html:

Holds all of the textboxes and buttons. 

games.js:

Takes the input from index.html and gives it to PHP.
Gets the games back, picks some random numbers and gives it back.
ID of textboxes is the name used for the indexes in the array.

index.php:
Takes the array of inputs from games.js

bgg.sqlite:
A file that holds the database.